# Create maps

Creating an immutable map:

```java
var map = Map.of(
  "key1", "value1",
  "key2", "value2"
)
```

Creating a mutable map:

```java
var map = new HashMap<>(Map.of(
  "key1", "value1",
  "key2", "value2"
))
```
